[Service]
ExecStartPost=/bin/sh -ec "if systemctl status --no-pager --lines=100 dnf-automatic-install.service| grep -q SMTP; then journalctl -r -p 0..3 -S -1w | mail -s "{{ inventory_hostname }}: Journal Entries" bex@pobox.com; shutdown -r +5 rebooting after applying package updates; fi"